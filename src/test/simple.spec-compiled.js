'use strict';

var chai = require('chai');
chai.should();

describe('Simple setup', function () {

    it('should success', function () {
        var a = true;
        a.should.equal(true);
    });
});

//# sourceMappingURL=simple.spec-compiled.js.map